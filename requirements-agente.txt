# Dependências extras do email_agent.py (agente de recebimento de Pedidos de
# Compra por e-mail) — não são necessárias pro dashboard em si, por isso
# ficam separadas do requirements.txt principal (que o Streamlit Cloud usa
# pra fazer o deploy do app).
#
# Autenticação é por delegação de domínio (reaproveita google-auth, já
# presente no requirements.txt principal, usado pelo gspread) — não precisa
# de google-auth-oauthlib, já que não há login interativo.
#
# Instalação: pip install -r requirements.txt -r requirements-agente.txt

google-api-python-client>=2.100.0
