# Credenciais — nunca subir para o GitHub
.streamlit/secrets.toml

# Dados persistentes locais — só o que é de baixo risco (cache/preferência de
# tema) continua em disco. Usuários e solicitações de acesso agora moram na
# planilha (ver data_handler.py), justamente porque o disco é efêmero.
.dashboard_config.json
.dashboard_changelog.json
.dashboard_snapshot.json
.dashboard_tema.json

# Python
__pycache__/
*.pyc
.env

# Planilhas locais
*.xlsx
*.xls
*.csv

# Materiais brutos de referência (logos originais em PDF/PNG @4x)
assets_ref/
smoke_out.txt
smoke_err.txt
_localtest/
